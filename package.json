{
  "name": "rickletoken-governance",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat tooling for compiling and verifying AhwaGovernance on BSCscan. Site itself is the build/ folder, deployed via GitHub Pages.",
  "scripts": {
    "compile": "hardhat compile",
    "verify": "hardhat verify --network bsc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-verify": "^2.1.3",
    "hardhat": "^2.28.6"
  }
}
